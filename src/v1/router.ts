import { Router } from 'express';

/**
 * Versioned public API.
 *
 * Endpoints will be migrated here in batches from ospex-agent-server.
 * This router is intentionally empty in the scaffold.
 */
export const v1Router: Router = Router();
